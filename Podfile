# platform *must* be at least 8.0
platform :ios, '8.0'

# flag makes all dependencies build as frameworks
use_frameworks!

pod 'Moya/ReactiveCocoa', '~> 6.0.0'

#framework that convers responses from/to json
pod 'ObjectMapper', '~> 1.1'